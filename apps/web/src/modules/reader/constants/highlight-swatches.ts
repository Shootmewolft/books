import type { HighlightColor } from '@/modules/reader/domain/highlight';

export const HIGHLIGHT_SWATCHES: Record<HighlightColor, string> = {
  brass: 'var(--color-brass)',
  patina: 'var(--color-patina)',
  rose: 'var(--color-cat-quality)',
  violet: 'var(--color-cat-architecture)',
};
